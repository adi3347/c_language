#include <stdio.h>

int main(){
    int n = 45;
    float m = 32.23;

    n = (int) m; // this is called typecasting basically we are coverting float to int and vice versa is also possible 
    printf("%d\n", n);
    
    return 0;
}
